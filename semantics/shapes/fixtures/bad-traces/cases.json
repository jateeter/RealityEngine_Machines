{
  "$comment": "Bad runtime traces for M5's third acceptance criterion: dynamic validation must fail AND produce a named violation record. Each case names the violation it exists to produce; validate-runtime-trace.py --fixtures fails if one does not fire, or fires without emitting a named re:SemanticGuardrailViolation individual. See README.md.",
  "cases": [
    { "graph": "write-outside-region.ttl",   "rule": "CW", "expect": "write-outside-declared-region" },
    { "graph": "unmapped-completion.ttl",    "rule": "CW", "expect": "unmapped-completion-write" },
    { "graph": "ambiguous-mapping.ttl",      "rule": "CW", "expect": "ambiguous-completion-mapping" },
    { "graph": "ambiguous-determination.ttl","rule": "CW", "expect": "ambiguous-determination" },
    { "graph": "event-without-run.ttl",      "rule": "CW", "expect": "event-outside-any-run" },
    { "graph": "forbidden-endpoint.ttl",     "rule": "CW", "expect": "forbidden-endpoint-use",
      "allowed": ["health", "rag_query", "/health", "/rag/query"] }
  ]
}
